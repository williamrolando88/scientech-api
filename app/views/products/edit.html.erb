<h1>Editing product</h1>

<%= render "form", product: @product %>

<br>

<div>
  <%= link_to "Show this product", line_brand_product_path(@line, @brand, @product) %> |
  <%= link_to "Back to products", line_brand_path(@line, @brand) %>
</div>
