<%= form_with model: [@line, @brand] do |form| %>
  <% if brand.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
        <% brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex flex-col gap-2">
    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name, class:'border px-2 py-1' %>
    </div>
    <div class='border w-fit px-2 py-1 bg-green-300'>
      <%= form.submit %>
    </div>
  </div>
<% end %>
